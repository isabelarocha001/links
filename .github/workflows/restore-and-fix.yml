name: restore-and-fix

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/restore-and-fix.yml'
      - 'scripts/**'

jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Hide VIP bot + public channel for non-PT
        run: python3 scripts/fix_hide_bot_non_pt.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.vue assets/css/links-page.css
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "fix: gringa so ve PrivSex + privados (sem bot VIP e canal publico)"
            git push
          fi

name: restore-and-fix

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/restore-and-fix.yml'
      - 'scripts/**'

jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply i18n + CTA below cards
        run: python3 scripts/fix_i18n_cta.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.vue assets/css/links-page.css utils/i18n.ts
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "fix: i18n multi-idioma + botao Entrar abaixo do card"
            git push
          fi
